# סטודיו מסות — בנייה גבוהה

**כתובת האפליקציה: https://anat1969.github.io/SkySkiper-GH/**

כלי תלת-ממדי שמפרק מגדל לבסיס, גוף וכותרת. אפשר להיכנס לכל גוש ולכל מקטע ולשנות את הפרמטרים שלו, והמודל מתעדכן מיד.

האפליקציה רצה כאן ישירות, בלי BASE44 ובלי שרת: כל דחיפה ל-`main` בונה ומפרסמת אותה מחדש דרך GitHub Actions. הפרויקטים נשמרים בדפדפן, וייצוא JSON מעביר אותם בין מכשירים.

## הרצה מקומית

```
npm install
npm run dev
```

## מה בריפו

| קובץ | תפקיד | איך משתמשים |
|---|---|---|
| `src/` | האפליקציה: מסכים, פאנל פרמטרים, חתך גושים ושמירה מקומית | `npm run dev` |
| `docs/02_prompts.md` | שישה פרומפטים לפי סדר | שולחים ל-BASE44 אחד אחרי השני |
| `docs/03_ui_spec.md` | גיליון-עזר: שלושת המפלסים, שש הקבוצות והיררכיית הכותרות | קוראים לפני ההתחלה, ומצרפים את הטבלה אם BASE44 סוטה |
| `entities/Project.json` | ישות פרויקט | מודבק בפרומפט 1 |
| `entities/SavedTypology.json` | ישות טיפולוגיה שמורה | מודבק בפרומפט 1 |
| `design/tokens.css` | שני לוחות צבע, טיפוגרפיה ומידות | נטען ע"י האפליקציה · מודבק בפרומפט 1 |
| `code/geometry.js` | מנוע הגאומטריה והנתונים המחושבים. נבדק על כל 8 הטיפולוגיות | נטען ע"י האפליקציה · מודבק בפרומפט 2 |
| `code/TowerViewer.jsx` | חלון תלת-ממדי: סיבוב, זום, הזזה, מבטים ובחירת גוש | נטען ע"י האפליקציה · מודבק בפרומפט 2 |
| `data/parameters.json` | כל הפרמטרים: קבוצות, תוויות, טווחים והסברים | הפאנל נבנה ממנו · מודבק בפרומפט 3 |
| `data/presets.json` | פרויקט ברירת מחדל ו-8 טיפולוגיות | נטען ע"י האפליקציה · מודבק בפרומפט 3 |

## למה החבילה בנויה כך

- **חיסכון בקרדיטים.** הגאומטריה והמציג כבר כתובים ובדוקים, ולכן BASE44 רק מחבר ממשק ולא ממציא מתמטיקה.
- **נתונים במקום קוד.** כל 53 הפרמטרים (12 ברמת בניין, 38 לכל גוש ו-3 סוגים) נמצאים בקובץ אחד. כדי להוסיף פרמטר מוסיפים שורה, בלי לבנות מסך.
- **דקדוק אחד לכל הגושים.** אותן שש קבוצות משמשות את הבסיס, הגוף והכותרת. מקטע יורש מהגוש ומשנה רק מה שנדרש.

## מה ב-MVP ומה בהמשך

**כלול:** שלושה מפלסי עבודה, שש קבוצות פרמטרים, 8 טיפולוגיות, עד 4 מגדלים עם גשר, מבטים קבועים, נתונים מחושבים, שמירה אוטומטית, ביטול, ייצוא PNG ו-JSON.

**בשלב הבא** (לפי האפיון המלא במסמך הקודם): מנוע הנחיות ועמידה, מצב השוואה, מצב מצגת לוועדה, צל לפי שעה ועונה, בניינים שכנים, ייצוא GLB, חיתוך חוצה ומעטפת מותרת.

## הערות

- **שתי דרכים להריץ את אותו כלי.** הכתובת למעלה היא האפליקציה העצמאית, וזו דרך המלך. מסלול BASE44 נשאר פתוח כחלופה: הפרומפטים והאפיון עדיין תקפים ומצביעים על אותם קבצים.
- מסלול BASE44 דורש התקנת `three` דרך הצ'אט ([תיעוד](https://docs.base44.com/Building-your-app/NPM-packages)). לא מצאתי אישור מפורש שהחבילה זמינה בכל תוכנית. באפליקציה כאן החסם הזה לא קיים.
- הערכים המספריים בטיפולוגיות הם נקודות פתיחה לדוגמה, לא נתונים של בניינים אמיתיים.
- השמות "סוג Seagram" ו-"סוג Turning Torso" מתארים טיפוסי מסה בלבד.
